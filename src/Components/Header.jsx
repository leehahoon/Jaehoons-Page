import styled from "styled-components";

const HeaderContainer = styled.header`
  width: 100%;
  border: 1px solid black;
`;

function Header() {
  return <HeaderContainer>헤더에오</HeaderContainer>;
}

export default Header;
